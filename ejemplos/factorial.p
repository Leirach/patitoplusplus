programa patito;

funcion int factorial(int num) {
    var int aux;
    desde i = 1 hasta num hacer {
        aux = num*(i)
    }
}

principal() {
    var int cont;
    quackin(cont);
    fibo(cont);
}